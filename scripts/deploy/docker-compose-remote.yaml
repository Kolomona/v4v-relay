services:
  bot:
    image: libre-relay-bot:latest
    container_name: libre-relay-bot
    restart: unless-stopped
    env_file: .env
    ports:
      - "7777:7777"
    volumes:
      - /home/hapa/dockers/libre-relay-bot/data:/app/data
      - /home/hapa/dockers/libre-relay-bot/.env:/app/.env:ro
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
    name: proxy-network
